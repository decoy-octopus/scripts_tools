# Super VIM enhancement
# Delete comments before sourcing it to your shell

set expandtab      # converts TAB key into spaces
set tabstop=2      # TAB key adds 2 spaces
set ic             # case-insensitive search inside file
set number         # display line numbers
set shiftwidth=2   # indentation (with > key) is set to 2 spaces